import {AbstractResource} from "./AbstractResource";

export type route = {
};

export class RouteResource extends AbstractResource {
    constructor(config: route) {
        super();
    }
}

export default RouteResource