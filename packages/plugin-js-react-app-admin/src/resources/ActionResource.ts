import {AbstractResource} from "./AbstractResource";

export type action = {
};

export class ActionResource extends AbstractResource {
    constructor(config: action) {
        super();
    }
}

export default ActionResource