<% var needConfigure = ('undefined' !== typeof configureService) && !!configureService -%>
<% var hasCustomConfigure = !!needConfigure && ('string' === typeof configureService) -%>
<%- pre_init({cnf: needConfigure && !hasCustomConfigure}) -%>
const <%- fnName -%> = <%- (service.substr(0, 1) === '@') ? `require('@ohoareau/microlib/lib/services/${service.substr(1)}').default` : `require('../services/${service}')` -%><%- needConfigure ? `(${hasCustomConfigure ? configureService : 'cnf'})` : '' %>.<%- method -%>;
<%- post_init() %>