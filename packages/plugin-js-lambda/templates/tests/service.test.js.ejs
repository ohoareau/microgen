<%- ('undefined' !== typeof test.mocks) ? include(`./_mocks.test.js.ejs`, {mocks: test.mocks}) : '' -%>
const service = require('../../<%- directory ? `${directory.split('/').map(() => '..').join('/')}/${directory}/` : '' -%><%- name -%>');

describe('', () => {
    it('', () => {
    });
});